import React from 'react';
import './Counter.css'
const Counter = (props) => {
    console.log(props);
    return (
        <div>
            <h1>This is Counter</h1>
        </div>
    );
};

export default Counter;